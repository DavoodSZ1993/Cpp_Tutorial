#include <iostream>
using namespace std;

int main(){

    // Arithmetic operators
    int x = 27, y = 17; // assignment

    int sum = x + y; // adition
    int sub = x - y; // subtraction
    int mul = x * y; // multiplication
    float div = x /y; // division
    int mod = x % y; // division remainder

    // comparison
    /*
    == : equals to
    != : not equal
    > : greater than
    < : less than
    >= : greater than or equal to
    <= : less than or equal to
    */

   // logical
   /*
   && : logical and
   || : logical or
   ! : logical not
   */

    return 0;
}